[package]
name = "basic-usages"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
nject = { path = "../../nject" }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
